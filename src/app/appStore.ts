import{createContext,useContext}from'react';import type{MinMaxDataset,OrderQtyEdit,OrderSettings,SupplierDataset,SupplierOverride}from'../domain/types';export interface AppState{minMax:MinMaxDataset|null;suppliers:SupplierDataset|null;overrides:SupplierOverride[];edits:OrderQtyEdit[];settings:OrderSettings;page:string;toast:string|null;loading:boolean}export interface Store{state:AppState;set:(p:Partial<AppState>)=>void}export const StoreContext=createContext<Store|null>(null);export const useStore=()=>{const x=useContext(StoreContext);if(!x)throw Error('Store missing');return x};
