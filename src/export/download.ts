import JSZip from'jszip';import type{Order}from'../domain/types';import{orderToCsv}from'./csv';import{safeFilename}from'./filenames';const save=(blob:Blob,name:string)=>{const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=name;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),0)};export function downloadCsv(o:Order){save(new Blob([orderToCsv(o)],{type:'text/csv;charset=utf-8'}),`${safeFilename(o.branch)}__${safeFilename(o.supplier)}__${new Date().toISOString().slice(0,10)}.csv`)}export async function downloadReadyOrdersZip(orders:Order[]){const z=new JSZip();for(const o of orders.filter(x=>x.status==='READY'))z.file(`${safeFilename(o.branch)}__${safeFilename(o.supplier)}.csv`,orderToCsv(o));save(await z.generateAsync({type:'blob'}),'Заказы_CSV.zip')}export{save};
